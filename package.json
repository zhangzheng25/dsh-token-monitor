{
  "name": "dsh-token-monitor",
  "version": "0.6.0",
  "description": "DeepSeek Harness plugin: a sidebar \"today's usage\" card that pops a token usage dashboard (today / 7 / 30 days, 30-day per-model stacked chart, model ranking with usage share, conversation stats). Data comes from the session corpus only (idempotent rebuild, no double counting), persisted across restarts.",
  "license": "MIT",
  "keywords": [
    "dsh",
    "deepseek-harness",
    "cordis",
    "plugin",
    "token",
    "usage",
    "billing"
  ],
  "main": "src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./client": "./client/bundle.js",
    "./package.json": "./package.json"
  },
  "dsh": {
    "bundle": {
      "patch": "./cordis.patch.yml"
    },
    "client": {
      "platform": "web"
    }
  },
  "files": [
    "src",
    "client",
    "cordis.patch.yml"
  ],
  "scripts": {
    "check": "node --check src/index.js && node --check client/bundle.js"
  },
  "engines": {
    "node": ">=20"
  }
}
